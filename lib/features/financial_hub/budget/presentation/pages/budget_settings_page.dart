part of 'mybudget_page.dart';

/// Settings / placeholder tab content extracted into its own file.
class BudgetSettingsPage extends StatelessWidget {
  const BudgetSettingsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const _PlaceholderContent(title: 'Settings');
  }
}

