/// Mock service for train search (placeholder for future API).
class TrainSearchService {
  /// Placeholder for fetching train results.
  Future<void> searchTrains(
    String fromCode,
    String toCode,
    String date,
  ) async {
    await Future.delayed(const Duration(milliseconds: 300));
  }
}
