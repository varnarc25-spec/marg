/// State/region for water bill (client filter; billers may include [stateId]).
class WaterState {
  final String id;
  final String name;
  const WaterState({required this.id, required this.name});
}
